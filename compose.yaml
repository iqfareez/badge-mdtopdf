services:
  badgemdtopdf:
    platform: linux/amd64
    image: badgemdtopdf
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
